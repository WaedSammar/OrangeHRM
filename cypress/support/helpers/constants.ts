export enum HTTP_METHOD {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
}

export enum HTML_TAGS {
  text = "text",
  body = "body",
  div = "div",
  span = "span",
  button = "button",
  a = "a",
  input = "input",
  textarea = "textarea",
  p = "p",
}

export enum SEPARATORS {
  COMMA = ", ",
  SPACE = " ",
  NEW_LINE = "\n",
  EMPTY = "",
}

export enum HTTP_STATUS_CODE {
  success = 200,
  created = 201,
  error = 500,
}
