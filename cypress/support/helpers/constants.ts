export enum HTTP_METHODS {
  GET = 'GET',
  POST = 'POST',
}

export enum HTML_TAGS {
  text = 'text',
  body = 'body',
  div = 'div',
  span = 'span',
  button = 'button',
  a = 'a',
  input = 'input',
  textarea = 'textarea',
}

export enum SEPARATORS {
  COMMA = ", ",
  SPACE = " ",
  NEW_LINE = "\n",
  EMPTY = "",
}